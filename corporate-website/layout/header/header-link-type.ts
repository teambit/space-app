/**
 * Represents a navigation link in the header
 */
export type HeaderLinkType = {
  /**
   * The text label for the link
   */
  label: string;

  /**
   * The URL or path the link navigates to
   */
  href: string;
};